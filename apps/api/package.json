{
  "name": "@movie-quotes/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "db:reset": "wrangler d1 execute movie-quotes --local --file=schema.sql --yes",
    "db:seed": "wrangler d1 execute movie-quotes --local --file=../../packages/pipeline/fixtures/seed.sql --yes",
    "db:reset:remote": "wrangler d1 execute movie-quotes --remote --file=schema.sql --yes",
    "db:seed:remote": "wrangler d1 execute movie-quotes --remote --file=../../packages/pipeline/fixtures/seed.sql --yes"
  },
  "dependencies": {
    "@movie-quotes/shared": "workspace:*",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "wrangler": "^4.77.0"
  }
}
